spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  application:
    name: manbanjalbu-bo-be

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:jdbc:mysql://192.168.10.214:3306/starbucks7th?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root1234}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:gnlaud07@gmail.com}
    password: ${MAIL_PASSWORD:auqe sdvy etia wkum}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        transport:
          protocol: smtp

server:
  port: ${SERVER_PORT:8081}

jwt:
  secret: ${JWT_SECRET:manbanjalbu-bo-local-dev-secret-key-min-256-bits-long!!}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

app:
  cors:
    allowed-origin-patterns: ${CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:*,http://127.0.0.1:*}
  mail:
    from: ${MAIL_FROM:${MAIL_USERNAME:no-reply@starbucks.local}}
    from-name: ${MAIL_FROM_NAME:스타벅스 고객센터}

logging:
  level:
    org.hibernate.SQL: debug
    org.springframework.security: info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: method
  api-docs:
    path: /v3/api-docs
